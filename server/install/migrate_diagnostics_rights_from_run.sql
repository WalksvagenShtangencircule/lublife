-- Ранее использовалось для копирования прав на отдельные методы. Сейчас все diagnostics/* кроме run/GET
-- завязаны на run через permissions_same — достаточно одной выдачи «diagnostics / run / GET».
-- Этот файл оставлен пустым намеренно (или выполните DELETE лишних строк в core_*_rights для старых aid, если мешают).

SELECT 1;
